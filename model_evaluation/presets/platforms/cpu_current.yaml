schema_version: '1.1'
id: cpu_current
device:
  adapter: cpu
runtime:
  adapter: cpu
backend_environment:
  provider: current
  profile: current
evaluation_environment:
  provider: current
  profile: current
