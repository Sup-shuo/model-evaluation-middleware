schema_version: '1.1'
id: nvidia_cuda_current
device:
  adapter: nvidia
  devices:
  - '0'
runtime:
  adapter: cuda
backend_environment:
  provider: current
  profile: current
evaluation_environment:
  provider: current
  profile: current
