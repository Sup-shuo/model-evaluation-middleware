schema_version: "1.3"

models:
  - id: mock-model
    label: Mock Model
    source:
      type: other
      ref: reference/mock-model
    provenance:
      policy: migration

benchmarks:
  - mock_demo

backend:
  profile: reference

evaluator:
  profile: reference_eval

offline: true

execution:
  mode: serial
  continue_on_error: false
